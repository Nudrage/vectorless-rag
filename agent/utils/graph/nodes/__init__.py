"""
Agent node implementations for the 3-node pipeline.

Each agent has its own module with both logic and system prompt:
    - planner.py: Document discovery and retrieval planning
    - retriever.py: Chunk retrieval execution
    - transformer.py: Content transformation and output formatting
"""
